Emacs configuration to load in ~/.emacs

Uses default Emacs' mnenomic mappings.
Left ALT is remapped to CTRL.
CAPSLOCK is remmaped to ALT.

So CTRL is under X key for left thumb finger.

"C-," other-window
"C-=" expand-region