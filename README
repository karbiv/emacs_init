Emacs configuration to load in ~/.emacs

Uses default Emacs' mnenomic mappings.

Main keys remapped by https://github.com/rvaiya/keyd
Left ALT to CTRL.
CAPSLOCK to ALT.

So CTRL is under X key for left thumb finger.

"C-=" expand-region
"C-," other-window
"C-`" kill-buffer (current-buffer)

Packages installed automatically from MELPA,
using builtin "package-selected-packages" list
and simple four line macro "conf".

Named Emacs sessions for different projects by "desktop-registry".